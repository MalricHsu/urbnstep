# 環境變數範本
# 複製此檔為 .env（本機開發用），並填入實際值：
#   cp .env.example .env
#
# Vite 會依模式自動載入對應的 env 檔：
#   npm run dev   → .env.development
#   npm run build → .env.production
# 機密請放到 .env.local 或 .env.*.local（已被 .gitignore 忽略）

# 後端 API 網址
VITE_API_URL=http://localhost:3010
